

export const upcoming = () => {
  return (
    <div>
      This will feature a check list of all the upcoming features to the site
      - Add the new upcoming page
      - Animations
      - To top button required for about me page
      - Improve Portfolio card designs
      - Add loading Animations
      - Add a login step so owner can update the site
      - Connect the site to a database to allow for dynamic content
      - Improve the CV page design
      - Improve css to have additional classNames for repeated styles (e.g. left margin)
    </div>
  )
}
